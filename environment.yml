name: gseav2
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - matplotlib>=3.7
      - numpy>=1.24
      - scipy>=1.10
      - pyyaml>=6.0
      - pytest>=7.0
